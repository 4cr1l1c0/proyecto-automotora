spring:
  application:
    name: ms-test-drive
  datasource:
    url: "jdbc:mysql://localhost:3306/db_test_drive?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: root
    password: ""
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  output:
    ansi:
      enabled: always
  flyway:
    enabled: true
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
    discovery:
      enabled: false

server:
  port: 9006

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html

logging:
  level:
    root: ERROR
    cl.duoc.ms_test_drive: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

feign:
  url:
    ms-clients: http://localhost:9001
    ms-vehicles: http://localhost:9004

eureka:
  client:
    enabled: false

resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: "jdbc:mysql://mysql-db:3306/db_test_drive?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

server:
  port: ${PORT:9006}

feign:
  url:
    ms-clients: http://ms-clients:9001
    ms-vehicles: http://ms-vehicles:9004
