Checklist:
Java 21 instalado
Maven instalado
MySQL 8.0 corriendo
pass for root = application.properties

  CREATE DATABASE db_clients;
  CREATE DATABASE db_sales;
  CREATE DATABASE db_employees;
  CREATE DATABASE db_vehicles;
  CREATE DATABASE db_inventory;
  CREATE DATABASE db_test_drive;
  CREATE DATABASE db_suppliers;
  CREATE DATABASE db_delivery;
  CREATE DATABASE db_branches;
  CREATE DATABASE db_insurances;

-Que es el proyecto

-MS:

  - ms-clients   (puerto 9001) 
  - ms-sales     (puerto 9002) 
  - ms-employees (puerto 9003) 
  - ms-vehicles  (puerto 9004) 
  - ms-inventory (puerto 9005) 
  - ms-test-drive(puerto 9006) 
  - ms-suppliers (puerto 9007) 
  - ms-delivery  (puerto 9008) 
  - ms-branches  (puerto 9009) 
  - ms-insurances(puerto 9011)  

  	Cambio de puerto         	Actualizar
  	ms-clients (9001)         	ms-sales, ms-delivery, ms-test-drive                 
	ms-vehicles (9004)         	ms-sales, ms-inventory, ms-test-drive, ms-insurances 
	ms-employees (9003)       	ms-sales                                             
	ms-branches (9009)        	ms-employees                                        
	ms-sales (9002)            	ms-delivery              

-Orden.

       ms-branches, ms-clients, ms-vehicles, ms-suppliers,
       ms-inventory, ms-insurances

       ms-employees  
       ms-test-drive 
       ms-sales  

       ms-delivery


-Importancia del orden.

-Explicar el flujo de conexión entre capas y microservicios.

-Verificar que se encuentren levantados.

